# Animation Plans

Plans generated by the `improve-animations` skill. Each is self-contained —
an executor with zero context from the conversation that produced it should
be able to implement one end-to-end.

| # | Title | Severity | Category | Status |
| --- | --- | --- | --- | --- |
| 001 | [Stagger the Symbol Wheel's arrow entrance and fix its movement easing](001-wheel-arrow-stagger-and-easing.md) | MEDIUM | Cohesion & tokens / Easing & duration | TODO |

## Execution order

Only one plan exists. No dependencies.

## Notes

- Plan 001 was written via `improve-animations plan <description>` (single
  described improvement, not a full audit) at commit `b808ed7`, in response
  to: "sembol çarkına daha profesyonel bir animasyon istiyorum."
- A full audit of the rest of the app's motion (`main.js`, `symbolmap.js`,
  the CSS outside `.wheel-*`) has not been run yet — this only covers the
  symbol wheel. Run `improve-animations` (bare) for a full-app pass if more
  plans are wanted later.
